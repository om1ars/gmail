import { Height } from '@material-ui/icons'
import React from 'react'

export const Sidebar = ({sidebarOpen}) => {
    return (
        <div style={{sidebarOpen ? Height}}>
            
        </div>
    )
}
